// Sass configuration
{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
      {
        "label": "Sass Compile 🛠",
        "type": "shell",
        "command": "sass scss/style.scss css/style.css -q --style=expanded --no-source-map",
        "group": "build"
      },
      {
        "label": "Sass Watch 👀",
        "type": "shell",
        "command": "sass scss/style.scss css/style.css -q --style=expanded --no-source-map --watch",
        "group": "build"
      }
    ]
  }